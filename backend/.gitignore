logs/
__pycache__/
*.pyc
# Caches dérivés du pipeline claims (extraction LLM + embeddings) — régénérables.
cache/*/claims.json
cache/*/claims_emb.npz
# Embeddings des cibles (knob α du bac à sable) — régénérables (embed nomic des cibles).
cache/*/target_emb.npz
# Cache des synthèses /insights (Markdown LLM par dataset/niveau) — régénérable.
cache/*/insights/
# Clé Mistral locale (lue par pipeline.cluster.mistral_client) — jamais commit.
.env
